
<div class="col-md-2">
  <h4>カテゴリ</h4>
  <ul class="nav nav-pills nav-stacked">
    <li role="presentation" class="active"><%= link_to "ALL", bookmarks_path %></li>
    <li role="presentation">
      <% @categories.each do |category| %>
        <%= link_to category_filter_path(:id => category.id) do %><%= category.id %>.<%= category.title %><% end %>
      <% end %>
    </li>
  </ul>
</div>
